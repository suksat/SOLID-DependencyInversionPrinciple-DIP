package dip;

class RTP_AdminUserService extends RTP_UserService {
    public boolean isValidUser(String username, String password) {
        // Check username and password validity for admin user
        // Return true if valid, false otherwise
        return true;
    }
}
