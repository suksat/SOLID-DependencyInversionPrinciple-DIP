package dip;

class RTP_NormalUserService extends RTP_UserService {
    public boolean isValidUser(String username, String password) {
        // Check username and password validity for normal user
        // Return true if valid, false otherwise
        return true;
    }
}
